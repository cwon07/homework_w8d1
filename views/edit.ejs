<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>POKEDEX</title>
        <%- include("import.ejs") %>
    </head>
    <body>
        <!-- ============= CONTAINER ============== -->
        <div class="container">
            <!-- sidebar -->
            <div class="sidebar">
            <!-- edit container -->
            <div class="edit">
                <h2>EDIT <%= pokemon.name %></h2>
                <!-- START FORM -->
                <form action="/pokemon/<%= id %>?_method=PUT" method="post">
                    <!-- fieldset for Pokemon info -->
                    <fieldset class="pokemon-info">
                        <!-- legend for Pokemon info -->
                        <legend>POKEMON INFORMATION</legend>
                            <!-- Pokemon's name -->
                            <label>NAME</label>
                            <input type="text" value="<%= pokemon.name %>"/>
                            <!-- Pokemon's type -->
                            <label>TYPE</label>
                            <input type="text" value="<%= pokemon.type %>"/>
                            <!-- Pokemon's Attack stat-->
                            <label>ATTACK STAT</label>
                            <input type="text" value="<%= pokemon.stats.attack %>"/>
                            <!-- Pokemon's HP stat -->
                            <label>HP STAT</label>
                            <input type="text" value="<%= pokemon.stats.hp %>"/>
                            <!-- Pokemon's Defense stat -->
                            <label>DEFENSE STAT</label>
                            <input type="text" value="<%=pokemon.stats.defense %>"/>
                    </fieldset>
                    <!-- submit button -->
                    <input type="submit" value="EDIT <%= pokemon.name %>"/>
                </form>
            </div>
            </div>
        </div>
    </body>
</html>